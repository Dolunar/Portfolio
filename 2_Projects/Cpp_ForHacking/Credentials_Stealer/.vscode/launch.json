{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Exécuter C++",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/main.exe",
            "preLaunchTask": "build cpp",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}
